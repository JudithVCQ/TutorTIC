# Server Configuration
PORT=3000
NODE_ENV=development

# Security Configurations
# Allowed origin for CORS (frontend estático)
ALLOWED_ORIGIN=http://localhost:5500

# JWT Configuration
# IMPORTANTE: Cambia este secreto por uno seguro en producción (mínimo 32 caracteres aleatorios)
JWT_SECRET=tutortic_2026_secret
JWT_EXPIRES_IN=8h

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tutortic_db
DB_USER=tutortic_user
DB_PASS=tutortic_password
